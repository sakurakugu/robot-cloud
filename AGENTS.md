## 项目概览

云端服务仓库，包含以下子项目：

| 目录     | 技术栈                                     | 说明               |
| -------- | ------------------------------------------ | ------------------ |
| `前端/`  | Vue 3 + TypeScript + Vite                  | 云端管理前端       |
| `后端/`  | Node.js + TypeScript + Express + WebSocket | 云端后端服务       |
| `nginx/` | Nginx                                      | 生产反向代理配置   |
| `tools/` | Python                                     | 本地热更新启动脚本 |

---

## 约定

- Node 使用 lint 和 typecheck
- 修改后要通过上述检查来防止编辑错误
- 开发阶段页面均为热更新，修改代码后无需重启服务，直接刷新即可验证
- 所有注释一律使用中文，回复也使用中文
- 数据库使用 15432 端口，后端使用 9000 端口
- 修改数据库，记得添加迁移文件
- 如需安装库，直接安装
- 该项目为自用项目，可以重构不用向前兼容
